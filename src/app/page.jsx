import { Header, SideBarNav } from '@src/components'
export const metadata = {
	title: 'Admin web',
	description: 'To manage fgito data',
}

const Home = () => {
	return (
		<Header/>
	)
}

export default Home
